{
  "name": "orbitfs",
  "version": "2.0.0",
  "description": "Mobile-first admin panel for the OrbitFS MCP server - full file control plus service monitoring/restart, so there's no reason to RDP into the VPS.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node --check setup.js && node --check license.js && node --check public/app.js && node --check public/addon-manager.js && node --check \"plugins/OrbitFS Sorter/server.js\" && node test-license.mjs && node test-license-ui.mjs"
  },
  "license": "ISC",
  "dependencies": {
    "docx-preview": "^0.4.0",
    "dompurify": "^3.4.12",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "highlight.js": "^11.11.1",
    "mammoth": "^1.12.0",
    "marked": "^18.0.6",
    "pdf-parse": "^2.4.5",
    "pdfjs-dist": "^6.1.200",
    "pg": "^8.22.0",
    "photoswipe": "^5.4.4"
  }
}
