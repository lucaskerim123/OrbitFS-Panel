{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if file_path contains .env, oauth_state.json, or credentials. If yes, refuse with: 🔒 Sensitive file — .env and tokens should not be edited in Claude. Use .env.example as template."
          }
        ]
      }
    ]
  }
}
